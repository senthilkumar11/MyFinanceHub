package com.ssk.myfinancehub.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Finance Theme Colors - Professional Blue Shades
val Primary80 = Color(0xFF1976D2)      // Rich blue for dark theme
val PrimaryGrey80 = Color(0xFF546E7A)  // Professional blue-grey accent
val Secondary80 = Color(0xFF42A5F5)    // Light blue for accents

val Primary40 = Color(0xFF0D47A1)      // Deep blue for light theme
val PrimaryGrey40 = Color(0xFF37474F)  // Dark blue-grey
val Secondary40 = Color(0xFF1E88E5)    // Vibrant blue

// Enhanced color scheme for better accessibility
val PrimaryContainer = Color(0xFFE3F2FD) // Light blue container (good contrast)
val OnPrimaryContainer = Color(0xFF0D47A1) // Dark blue text (high contrast)

val SecondaryContainer = Color(0xFFE1F5FE) // Light blue container
val OnSecondaryContainer = Color(0xFF01579B) // Dark blue text

// Light theme specific improvements
val PrimaryContainerLight = Color(0xFFE1F5FE) // Slightly darker light blue for better contrast
val OnPrimaryLight = Color(0xFFFFFFFF) // Ensure white text on primary

// Status Colors - Keep green for income, use blue accent for info
val IncomeGreen = Color(0xFF4CAF50)    // Keep green for income (universal money color)
val ExpenseRed = Color(0xFFE53935)     // Red for expenses (better contrast)
val WarningOrange = Color(0xFFFF8F00)  // Orange for warnings (better contrast)
val InfoBlue = Color(0xFF1976D2)       // Blue for info (matches theme)

// Neutral Colors - Enhanced for theme support
val SurfaceLight = Color(0xFFFAFAFA)   // Very light grey
val SurfaceDark = Color(0xFF121212)    // Material dark surface
val OnSurfaceLight = Color(0xFF1A1A1A) // Almost black for text
val OnSurfaceDark = Color(0xFFE0E0E0)  // Light grey for dark theme text

// Card and elevated surfaces
val ElevatedSurfaceLight = Color(0xFFFFFFFF) // Pure white
val ElevatedSurfaceDark = Color(0xFF1E1E1E)  // Elevated dark surface